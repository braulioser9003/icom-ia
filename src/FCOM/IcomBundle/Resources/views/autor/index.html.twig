{% extends 'base.html.twig' %}

{% block body %}
    <h1>Autors list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Apellidos</th>
                <th>Correo</th>
                <th>Perteneceinstitucion</th>
                <th>Institucion</th>
                <th>Estado</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for autor in autors %}
            <tr>
                <td><a href="{{ path('autor_show', { 'id': autor.id }) }}">{{ autor.id }}</a></td>
                <td>{{ autor.nombre }}</td>
                <td>{{ autor.apellidos }}</td>
                <td>{{ autor.correo }}</td>
                <td>{% if autor.perteneceInstitucion %}Yes{% else %}No{% endif %}</td>
                <td>{{ autor.institucion }}</td>
                <td>{% if autor.estado %}Yes{% else %}No{% endif %}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('autor_show', { 'id': autor.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('autor_edit', { 'id': autor.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('autor_new') }}">Create a new autor</a>
        </li>
    </ul>
{% endblock %}
